apiVersion: v1
kind: ConfigMap
metadata:
  name: dnsmasq-cm
data:
  # Configuration values can be set as key-value properties
  #database: mongodb
  #database_uri: mongodb://localhost:27017
  NATS_HOSTPORT=localhost:4222
